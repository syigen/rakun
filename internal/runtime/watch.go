package runtime

func (runTime *RunTime) Watch() {
	//pubsub := runTime.Environment.CommServerClient.Subscribe(runTime.Context, "mychannel1")
	//
	//ch := pubsub.Channel()
	//
	//for msg := range ch {
	//	log.Println(msg.Channel, msg.Payload)
	//}
}
